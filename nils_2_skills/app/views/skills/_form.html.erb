<%= form_for (@skill) do |s| %>

  <% if @skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= s.label :skill_name %>:
  <%= s.text_field :skill_name, class: 'form-control'%><br/>

  <%= s.label :category %>:
  <%= collection_select(:skill, :category, SkillCategory.all, :id, :category_name) %>
  <%= link_to "Create a new category", new_skill_category_path %><br/>

  <%= s.label :description %>:
  <%= s.text_area :description, class: 'form-control'%>

    <div class="actions">
    <%= s.submit %>
  </div>

<% end %>